# Essential web dependencies - Python 3.11 compatible
Flask==2.3.3
flask-cors==4.0.0
gunicorn==21.2.0
pymongo==4.6.1

# Build tools
setuptools>=68.0.0
wheel>=0.41.0
Pillow==10.1.0
Werkzeug==2.3.7

opencv-python-headless==4.8.1.78
# Note: dlib and face-recognition are installed separately above
# Note: torch and torchvision are installed separately above